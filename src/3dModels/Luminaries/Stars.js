import { Luminaries } from "./Luminaries";
import * as THREE from 'three'

export class Stars {
    // createModel = () => {
    //     const starGeometry = new THREE.BufferGeometry()
    //     for(let i =0; i < 6000; i++) {
    //         const star = new THREE.Vector3(
    //             Math.random() * 600 - 300,
    //             Math.random() * 600 - 300,
    //             Math.random() * 600 - 300,
    //         )
    //         starGeometry.attributes.
    //     }
    // }
}