export class Luminaries {
    constructor(radius, witdhSegment, heightSegment) {
        this.radius = radius
        this.witdhSegment = witdhSegment
        this.heightSegment = heightSegment
    }
}
